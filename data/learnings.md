# Learnings

## 2026-07-10 — Overtrading looks like the bigger lever than stop/TP tuning

**What the data says:**
- Backtest (Jan 8–Jul 7, 35 closed trades): -18.2% vs SPY +9.0%. Win rate 40%, avg win +10.2%, avg loss -9.9%. At that win rate a ~1:1 payoff is a losing expectancy (~-1.9%/trade) — the edge problem is win rate, not payoff skew.
- Exit mix is lopsided: 25/35 trades (71%) hit the trailing stop (sum -143.1pp), only 3/35 (9%) reached the 25% take-profit (sum +86.5pp), and 7/35 (20%) rode to max_hold (45d) averaging -1.2% — dead money that neither worked nor got cut.
- Tune sweep confirms the *current* stop/TP (8%/25%) sits in the most robust region tested: avg return -0.7%, worst-case -2.6% across min_score variants — far better worst-case than e.g. 12%/15% (worst -18.6%) or 15%/20% (worst -14.6%). So stop/TP are not the prime suspect.
- Strongest pattern in the sweep: for every single stop/TP pair tested, buys_wk=2 underperformed buys_wk=1 by 2-4pp (e.g. 8%/25%: +1.3% at 1 buy/wk vs -2.6% at 2 buys/wk). More trades per week made results worse in 100% of tested combos.
- Caveat: live config's `max_buys_per_week` is 20 — the sweep only ever tested 1-2/week, so the actual live cap has never been validated by tuning. Live decision feed is too thin right now (14 decisions, fresh paper reset) to say which confluence check rejects the most candidates — 6/8 skip_buys failed on `unusual_volume`, but n=8 is not enough to act on alone.

**Proposed change:** Lower `buying.max_buys_per_week` from 20 down to something inside the validated range (e.g. 4) as a bounded test. Thesis: every stop/TP combination in the tune sweep got worse when trade frequency doubled from 1 to 2/week, and the live cap of 20 is far outside anything the tuning process has actually confirmed. Forcing more selectivity per week should concentrate capital in the highest-score/highest-confluence setups rather than filling the book, which may lift the 40% win rate toward the ~45-50% needed for the current payoff ratio to be profitable. Re-run the tune sweep with buys_wk values up to 5-6 to extend the validated range before committing further.

## 2026-07-10 (later) — min_composite_score barely filters anything; insider is nearly the whole score

**What the data says:**
- In `tune_results.json`, 31 of 40 (stop, tp, buys_wk) buckets return byte-identical trades/win-rate/return whether `min_score` is 2.5, 3.0, or 4.0 — raising the threshold 60% removes zero candidates in most regions. The 9 buckets where it does matter are almost all in already-poor stop/tp regions (12%/15-30% stops), not near the current config.
- Every one of the 26 live `scan_candidates` rows this week has `insider` as by far the largest score component (2.0–5.0 of a 2.9–6.8 total); `sector` adds a small amount (0–0.9); `fundamentals`/`news`/`breakout` are nonzero in only 3, 2, and 2 rows respectively; `reddit` and `events` never appear at all despite carrying configured weights (0.75, 1.0).
- Mechanically this checks out: `insider_score()` (bot/signals/insider.py) is capped at raw 3.0 (×`insider_weight` 2.0 = 6.0 max), and a single insider buying ≥$250k alone already scores 2.5 raw → 5.0 — above the current `min_composite_score` of 2.5 with no help from any other signal. AVO and VII-UN both hit the 5.0 insider-only ceiling this week.
- Net effect: the composite score, as actually populated, is functioning close to "insider hit or not" rather than a true multi-signal confluence score — `min_composite_score` at 2.5 (or even 4.0) doesn't force any corroboration from sector/fundamentals/news/breakout.
- Caveat: sample is thin (26 candidates from 3 scan runs, only 2 closed... 0 closed live trades) — can't yet say whether reddit/events being silent is a real "quiet week" or a pipeline gap; `signal_rewards` is empty (0 rows) simply because no live paper position has closed yet since the 7/7 reset, so the learning loop's "no adjustments" today is expected, not a bug.

**Proposed change:** Raise `buying.min_composite_score` from 2.5 to ~5.5 as a bounded test — above the single-insider-only ceiling of 5.0, so a candidate must get corroboration from at least one other signal (sector/fundamentals/news/breakout) to qualify, not insider alone. Thesis: this directly targets the "insider is nearly the whole score" pattern above without touching any individual signal weight. Re-run the tune sweep with finer min_score steps (4.5, 5.0, 5.5, 6.0) to find where the threshold actually starts cutting trades, since 2.5→4.0 barely moved anything.

## 2026-07-11 — min_score=5.5 is the weakest tested threshold in its own locked-in stop/TP cell

**What the data says:**
- `tune_results.json` now has the finer 4.5/5.0/5.5/6.0 sweep requested last night (320 rows). Bucket-level robustness across all stop/TP/buys_wk combos: min_score 4.5 has the best average (+2.5%) and median (+3.9%) return; 5.5 (current) is the *second-worst* bucket on average (+0.6%, median -0.4%); 6.0 has the worst average (-2.2%) but contains the single best row in the whole sweep.
- That best row is exactly the current config's other three knobs: stop=10%, tp=20%, buys_wk=4 — at min_score=6.0 it returns +26.9% (win rate 54%, max DD 11.2%) vs. +8.1% (win rate 41%, DD 19.1%) at min_score=5.5, the value actually deployed. Same stop/TP/frequency, one score-threshold step up, 3x the return and roughly half the drawdown.
- Confirms last night's separate finding that stop=10/tp=20 is the most robust pair on both axes (highest per-bucket average of any stop or TP value tested) — so the stop/TP half of config is not in question, only where min_score sits within that cell.
- Live decision log (9 confluence-scored candidates since 7/7 reset) still shows `unusual_volume` failing 8/9 times, including on both live buys (AVO, WRAP) — same pattern flagged last night (was 6/8), now firmer at n=9 but still thin; worth a look at whether the RVOL threshold is calibrated for the sub-$20 illiquid names this bot actually scans.
- `signal_rewards` and `catalyst_rewards` are both still 0 rows — no live paper position has closed since the 7/7 reset (only 5 trades total, all opens), so signal-quality and news-catalyst-quality questions remain unanswerable from live data; the 60-trade backtest has no signal/catalyst tags to fall back on either (engine-only backtest, per its own caveats).

**Proposed change:** Bump `buying.min_composite_score` from 5.5 to 6.0, holding stop/TP/buys_wk fixed at their current (already-validated-robust) values. Thesis: within the exact cell the bot is running today, 6.0 triples return and nearly halves drawdown vs. 5.5 in the sweep — and it stays consistent with the original design intent (forcing corroboration above the single-insider ceiling of 5.0), just requiring a slightly stronger second signal. Caveat: 6.0 is not robust *on average* across other stop/TP combos (worst bucket tested), so this is a bet on the specific cell already in use, not a claim that 6.0 is universally better — re-check after ~15-20 more live closed trades to see if the live win rate moves toward the 54% the sweep implies.

## 2026-07-11 (later) — Re-buying a name right after its take-profit looks costlier than re-buying after a stop-out

**What the data says:**
- Exit mix, 60-trade backtest: 45/60 (75%) hit the trailing stop (avg -5.6%, win rate 24.4% — expected, most stops are cut losers), 11/60 (18%) hit take-profit (avg +26.8%, 100% win by definition), 4/60 (7%) rode to max_hold (avg +4.2%, 75% win — not "dead money," a mild net positive).
- Winners are held ~2x longer than losers (avg 20.7 days vs 10.4 days) — stops cut losers fast, winners are left to run. Healthy asymmetry, no action needed.
- 13 tickers were re-bought after a prior trade closed. Split by why the prior trade closed: re-entries after a stop-out (n=14) win 42.9% (avg +4.1%) — statistically indistinguishable from the 43.9% win rate on fresh first-entries (n=41), i.e. a stop-out doesn't poison the next entry. Re-entries after a take-profit (n=5) win only 20% (1/5): CABA -5.0%, ONCY -10.3%, STIM -11.0%, LODE -10.3%, one STIM +25.3% winner.
- Mechanistically plausible: a stop-out doesn't mean the thesis was wrong, so re-scanning behaves like a fresh trade; a take-profit means the easy move already happened, so buying the same name back is chasing extension — the weaker setup here.
- Caveat: n=5 is too small to act on (one trade swings the rate 20pp) — this is a pattern to watch, not a confirmed edge.

**Proposed change:** No config change yet — flag for tracking. If the post-take-profit re-entry underperformance holds at larger n (re-check after 3-4x more such re-entries accumulate), consider requiring a fresh/updated catalyst before re-buying a ticker within ~10-15 trading days of taking profit on it, rather than letting a plain rescan hitting the same score re-open the same name.

## 2026-07-14 — Tune sweep is silently broken (0 trades in all 320 combos); auto-tune applied a coin-flip change. RVOL backlog item: rejected by our own data.

**What the data says:**
- `tune_results.json`'s entire 320-row sweep (stop×tp×buys_wk×min_score) returns `trades: 0, return_pct: 0.0` on **every single row**, including stop=8/tp=15/buys_wk=4 — the exact combo `backtest_report.json` shows producing 78 real trades and +25.6% over the same broad window. The sweep engine is not finding any candidates/trades right now; this is a bug, not a null result.
- `learning.auto_apply_tuning=true` nonetheless fired and changed live config (stop 15→8%, tp 25→15%, buys/wk 6→4) picking a "robust median +0.0%" region out of a dataset where literally every cell is tied at zero — that's not evidence, it's noise dressed as a decision.
- `walkforward.json`'s own verdict already says `edge_survives_out_of_sample: false` and only 1 of 6 OOS-tested combos had any trades at all (10 test trades) — consistent with the same underlying sweep bug bleeding into walk-forward too. **Do not trust any tune/walkforward-derived parameter conclusion until this is fixed.**
- **RVOL backlog item — verdict: reject as a score input.** Cross-referencing `screens.json`'s per-ticker lookup (n=2,080, median RVOL 0.49) against every live buy ever recorded (WRAP×2, NTSK, UUUU×2, AVO, CAI): all sit at RVOL 0.19-0.83, and **all six failed the `unusual_volume` confluence check** (rvol_min 1.5). None are within reach of the `rvol_top` screen (starts at 18x). This strategy's edge is quiet insider accumulation, not volume breakouts — adding RVOL as a positive score input would fight its own DNA. The `unusual_volume` confluence check itself is currently decorative (0/6 pass rate, never the blocker since min_checks_passed=4 is still met).
- Sector cap validated: decision log shows WRAP rejected 17 consecutive times solely on "max positions in Technology" despite scoring 11.03/10-checks-7. Directly supports today's separate call to disable `max_sector_positions`.
- Data quality: 20 issues / 38 tickers checked, **0 on held positions** — mostly 5-day-stale bar cache or small source mismatches on non-held names (WRAP, EVMN, LRMR, IMRA); SSACU/AIJTY flatlined (likely illiquid/halted). Book integrity looks fine.
- Signal/catalyst quality still unanswerable live: `signal_rewards` has only 10 rows from one closed trade (WRAP, -4.04%); `catalyst_rewards` is empty. Too thin to conclude.
- Forum mining: an arXiv microcap insider-signal study (2602.06198) finds insider buys disclosed into price **strength** near 52-week highs outperform those into weakness — opposite of a "buy the dip" instinct. Worth reconciling against the 52w-high-proximity backlog item next session using `screens.json`'s existing `hi52_proximity` field.

**Proposed change:** Set `learning.auto_apply_tuning` to `false` until the tune sweep is debugged and produces nonzero trades again. Thesis: auto-applying "best of" a dataset where every row is a tied zero is worse than freezing config, since it silently drifts live parameters (stop/tp/buys_wk already moved twice off zero-signal data) while looking like a principled, data-driven update. Re-enable only after confirming a rerun of the sweep produces the same trade counts as the main backtest for matching parameters.

## 2026-07-14 (night 2) — Walkforward engine now PASSES, but tune sweep is still dead and auto-tune fired again on zero-signal data; DVLT (an explicit avoid-list name) is a live position

**What the data says:**
- `walkforward.json` regenerated with a real OOS test (see commit "walkforward: native rerun"): top combo (stop=8%/tp=15%/2 buys-wk/min_score=4.5) trained median +6.76%, tested +4.89% on unseen 5/18-7/9 data vs SPY +2.03% — `edge_survives_out_of_sample: true`. But its own `monte_carlo_1000x` bootstrap gives `p5_pct: -12.5%` (negative) — a 1000x resample of the trade sequence says a meaningfully bad outcome is still plausible. Treat the OOS pass as fragile, not proven, and `backtest_report.json`'s +25.6% headline as an upper bound (6.1% of insider-active tickers are now unpriceable/delisted and absent from the sim).
- `tune_results.json` (the 320-row full grid) is **still** completely broken — 0/320 rows show any trades, unchanged from last night — and it now directly contradicts walkforward: the identical combo (stop=8/tp=15/buys_wk=2/min_score=4.5) shows 0 trades in tune_results but 12 real test trades in walkforward.json. Two engines, one fixed, one not — do not trust tune_results.json for anything until it's debugged the same way walkforward was.
- Despite that, `learning.auto_apply_tuning` fired **again** today at 06:05 (decisions id 139, "robust median return +0.0%") and moved live stop/tp/buys_wk off the still-broken zero-signal sweep — the exact bug flagged last night. Last night's proposal to disable `auto_apply_tuning` was not applied; config.json still has it `true`. Second consecutive night it silently drifted live parameters off noise.
- **Flagging loudly**: DVLT is an open live position (18.28 sh @ $0.36, wildcard sleeve) but sits on `research.json`'s own avoid list (serial diluter, 855M shares out +1518% YoY, meme-coin/tokenization pump, no durable catalyst — logged 2026-07-08/09). Decision log shows it wasn't bought by the scorer: it was `recon_adopt`-ed in from the T212 demo broker as a pre-existing holding the ledger didn't know about. Broker is account of record, but this directly contradicts the research desk's own call — human should decide whether to exit it.
- 52w-high-proximity backlog item (this session's rotation): cross-referenced `screens.json`'s `hi52_proximity` (0=52w low, 1=52w high; universe median 0.625, n=2,080) against the 5 live buys — only AVO sits near its high (0.87); NTSK/UUUU/CAI/WRAP all sit mid-range (0.43-0.50). Quantpedia's public writeup confirms the academic direction (closer-to-high outperforms) but flags its own OOS backtest shows the effect decaying out-of-sample. Verdict: directionally plausible but n=5 is nowhere near enough to act on, and our current buys skew away from the "buy strength" pattern the literature favors — watch, don't act.

**Proposed change:** Set `learning.auto_apply_tuning` to `false` (repeating last night's proposal, now with a second occurrence as evidence). Thesis: whatever fix separated walkforward.json's engine from the tune-sweep script needs to land before auto-tune is trusted with live capital — freezing it is strictly safer than a third silent zero-signal parameter change. Separately, human should sanity-check the DVLT position against the standing avoid-list call.

## 2026-07-14 (night 3) — Exit-window conflict adjudicated: hold 12/25, blame the regime not the process; DVLT flag resolved by account reset; tune sweep looks fixed

**What the data says:**
- Exit-window adjudication: `tune_results.json` (896 rows, real nonzero trade counts again — the two-night "0 trades" bug is gone) and `walkforward.json`'s 8-week OOS both cleanly favor tight exits: (stop 8, tp 15) is the best-average cell (+3.8% avg, n=16) while (15, 25) averages -7.1% (max only +1.3%) in the *same* sweep. That contradicts the full 12-month `backtest_report.json` (-11.5%, run on 8/15-era config) behind config's window-conflict note. Root cause looks like regime, not a bug: `quant_regime.json`'s trace shows the tune/OOS window sits inside calm_drift/trending (vol 14.1% ann.), while the full-year window spans stress stretches where an 8% stop plausibly whipsaws out before a 25% TP is reached.
- Forum check backs that read: ATR/volatility-adaptive stops (tight in calm regimes, wide in stress) are the standard practitioner fix for exactly this "one fixed pair wins in one regime, loses in another" pattern — not picking a single static stop/TP. [Stop-Loss Strategies for Algo Trading](https://blog.traderspost.io/article/stop-loss-strategies-algorithmic-trading), [ATR-Based Stop-Loss for High Volatility Breakouts](https://www.luxalgo.com/blog/atr-based-stop-loss-for-high-volatility-breakouts/).
- Today's regime call is itself contested: `research.json` says risk_off (fresh Iran ceasefire collapse/oil-shock, event-driven), but `quant_regime.json`'s trailing KMeans still reads calm_drift and its HMM reads trending (`hmm_agrees_with_kmeans: false`) — the news-driven call is simply ahead of the trailing price-based models; too early to score which was "right" since today's shock hasn't shown up in 20-day price action yet.
- Account reset resolves last session's loud flag: all 5 open positions (AVO, CAI, NTSK, UUUU, DVLT) show `recon_close` today (absent at broker, closed manually in the T212 app), coinciding with the demo top-up to £500/$670 (config's 2026-07-14 resize note). DVLT — flagged last night as a live position contradicting its own avoid-list call — is simply gone now, not a live contradiction. `auto_tune` fired once more at 06:05 (decision 139) before the `auto_apply_tuning` pause took effect later in the day; decision 146 (11:36, "no adjustments") confirms the pause is now actually working.
- Cross-sectional momentum backlog item (this session's rotation): `screens.json` now has `mom13w_rank` per ticker. The 5 live buys ever recorded span the whole range (UUUU 12.7, AVO 27.8, CAI 28.0, WRAP 49.3, NTSK 81.2) — no skew toward high momentum, but n=5 and none closed organically (all closed via today's account reset, not stop/TP), so there's no real win/loss tag to check the ranks against. Verdict: can't validate either way this session; revisit once the reset account accumulates organic exits.
- Signal/catalyst quality still unanswerable: `signal_rewards` has 10 rows from one trade (WRAP, -4.04%); `catalyst_rewards` is still empty (0 rows). Nothing to conclude yet.

**Proposed change:** Keep `selling.trailing_stop_pct`/`take_profit_pct` at the current 12/25 overlap-robust middle — do not revert to 8/15. Thesis: the short-window data (tune sweep + 8-week OOS) was generated inside an unusually calm regime and isn't representative of the fuller regime mix we may be re-entering now (fresh Iran-driven risk-off). Separately, once auto-tune is reconsidered, have it rank stop/TP cells by min(6-month return, 12-month return) — worst-case-of-windows — rather than either window alone, so a pair must survive both the calm and the stressed regime before it's allowed to move live exits.

## 2026-07-15 — Lorentzian & FinBERT both fight the insider-into-weakness DNA; vol-target sizing would tax our best tier — attack vol via the STOP, not the size

**What the data says:**
- **Lorentzian k-NN (this session's validation):** the 4-day-forward model scores 4 of 6 ever-live buys NEGATIVE (UUUU -0.75, WRAP -0.5, AVO -0.25, YEXT 0.0; NTSK/CAI absent from the 2,062 pond). Same shape as the RVOL (7/14) and 52w-high (7/14 n2) findings: our edge is quiet insider accumulation into weakness/mid-range, so a short-horizon momentum model votes bearish on exactly our entries. **Verdict: reject as a confluence/veto input — it would reject the strategy's own setups.** Paper-cohort it before any score use.
- **FinBERT:** still unvalidatable — the only organically-closed trade is WRAP (-4.04%) and its FinBERT mean is ~0.02 (noise). Its "worst" WRAP headline (-0.85) is a junk *"Unusual volume stocks in Monday's session"* listing headline, not sentiment — confirms the model ingests non-signal headlines. **Keep display-only** until organic exits accumulate (all 5 prior positions closed via the 7/14 account reset, not stop/TP, so no outcome tag exists).
- **Vol-target sizing (backlog rotation, WebSearch'd):** 98-trade backtest dispersion by entry-price tier (vol proxy): <$3 → stdev 22.8 / avg +8.7% / 60% win; $12-20 → stdev 12.7 / avg +2.5% / 55% win. The wild cheap tier is *also the highest-return tier* — inverse-vol sizing would downsize exactly where the edge is strongest. Literature agrees vol-targeting is a **drawdown-smoothing tool, not a return booster** ([QuantifiedStrategies](https://www.quantifiedstrategies.com/volatility-based-position-sizing/), [TradersPost](https://blog.traderspost.io/article/position-sizing-algorithms)). Caveat: survivorship (7% of insider names unpriceable) inflates the cheap tier most, so its true edge is below +8.7%. **Net: don't adopt inverse-vol SIZING — it fights the return engine.**
- **Data-trust audit (held tickers):** all 3 broker-disagrees flags are held names driven by a stale bar cache (last 7/8): WRAP broker $2.16 vs cache $1.55 (+38.9%), NTSK $14.4 vs $12.61 (+14.2%), YEXT $5.43 vs $5.22 (+3.9%). Broker is account of record → **trust the broker prices; the cache is stale, not corrupt.** No held name is in the 98-trade sim, so no backtest rows are implicated. JEM cache $0.386 vs Yahoo $6.04 is a bad ticker-map (non-held) — ignore for the book.
- **Exit mix (98 trades):** stop 40 @ -11.8%, max_hold 40 @ +6.7%/88% win, take_profit 18 @ +29.8%. Stops are the only losing bucket — reinforces that the stop, not the TP, is the lever to work.

**Proposed change:** Replace the flat 12% trailing stop with a **volatility-scaled stop**: `stop_pct = clamp(1.5 × 20d_realized_vol, 10%, 20%)`, risk% held constant so size auto-shrinks as the stop widens. Thesis: a flat 12% stop sits ~1.9σ of daily noise out for a $12-20 name but *inside* 1σ for a <$3 name (stdev 22.8), so cheap names eat noise-driven stop-outs the pricier tier never sees — widening the stop (not shrinking the winning tier) is the return-preserving way to address volatility, and it's the same ATR-adaptive direction the 7/14-n3 exit-window adjudication already pointed to. Bounded 10-20%, touches no veto. Validate by adding a vol-scaled-stop column to the tune sweep and ranking it against the flat-12% baseline (worst-case-of-windows) before it may touch live exits.

## 2026-07-15 (night 2) — Exit-window conflict revisited: TP, not stop, is the lever; the 12-mo window is a pre-break regime we're no longer in. Cut TP.

**What the data says:**
- **Adjudication (item 8), with new CUSUM evidence:** `quant_regime.json` puts the last structural break at **2026-06-10** and flags `tune_window_clean:false` — the 6-mo tune window straddles the break, so pre-6/10 rows should be down-weighted. The regime NOW is unambiguously **calm_drift** (SPY vol20 14.1% ann., 6 straight calm_drift days, breadth 48.9). The 7/14 risk_off (Iran) that justified holding wide exits on 7/14-n3 **never reached price** one day on — so the "we might re-enter stress" premise has weakened. Verdict: the **recent/post-break window reflects NOW**; the full-12-mo window (which liked wide 15/25) is a stale higher-vol regime.
- **TP is the lever, not the stop.** In the 896-row sweep, holding stop=12 and only cutting TP: 25→20→18→**15** moves mean return **-3.9 → -1.7 → -0.5 → +1.7%**. Every tp25 cell is negative-mean; the robust winner is stop9/tp15 (mean +6.0, best worst-case -6.6). Mechanism: in a 14%-vol drift a +25% TP rarely prints, so winners round-trip back into the stop. The current **12/25 is a bottom-tier cell here (mean -3.9), not the "overlap-robust middle" the config note claims.**
- **Walkforward** passes OOS (top combo test +7.08% vs SPY +2.03%, `edge_survives_out_of_sample:true`) but its Monte-Carlo **p5 = -11.4% (negative)** — treat the edge as fragile, hinging on a few trades; and backtest +40.3% is an upper bound (7.0% survivorship gap).
- **Data trust (held names):** WRAP broker $2.16 vs cache $1.55 = **+38.9% drift** (also stale to 7/8) — broker is account of record, **trust the broker, the cache is stale not corrupt**; same for NTSK (+14.2%) and YEXT (+3.9%). JEM cache $0.386 vs Yahoo $6.04 is a bad ticker-map (non-held) — ignore. Given WRAP's cache is this wrong, distrust any WRAP backtest rows too. Critic vetoed WRAP 5× (dilution/promotional narrative) yet it's still a live buy — worth a human look.
- **Piotroski/Altman (new item 5g):** `piotroski_weight` is live but **0 z_veto have fired** and no organic exits exist yet, so neither the F-score edge nor Z-veto false-positive rate is testable this session. `signal_rewards` still 10 rows/1 trade (WRAP -4.04%), `catalyst_rewards` still empty — signal/catalyst quality remains unanswerable.
- **Backlog rotation — mean-reversion guard:** all 6 live buys sit at RVOL **0.22–0.83** (below-average volume, quiet accumulation) and mid-range hi52 (0.43–0.87), so a "day-1 ±25% move **without catalyst** = veto" would essentially never touch a core entry — but it directly guards the **wildcard/pennystock sleeve** where hype pumps live (JZXN +102% on an unsigned MOU at 90× float). Forum mining confirms unsupported single-day spikes reverse next-day when volume doesn't back them. Verdict: **good fit as a wildcard-sleeve veto**, low risk to the core; note partial overlap with the existing `max_extension_pct:25` check (that's cumulative extension, not a single-bar spike). Worth building.

**Proposed change (one, bounded):** Cut `selling.take_profit_pct` **25 → 18** (keep `trailing_stop_pct` at 12, untouched — avoids reopening the whipsaw debate). Thesis: in the confirmed calm_drift regime a 25% TP rarely fills and winners give it back, so the TP — not the stop — is what's bleeding the current cell (12/25 mean -3.9 vs 12/18 -0.5 vs 12/15 +1.7 in-sample). 18 is a two-notch step toward the regime-appropriate tight target without fully committing to the calm-only 15; if calm_drift persists another 2–3 weeks, go to 15. Process fix stands: before auto-tune may touch exits, rank cells by **worst-case-of-windows with post-6/10 sub-window weighting** (the CUSUM break makes the 6-mo window not clean). Validate the TP cut on the next batch of organic exits.

## 2026-07-15 (night 3) — The binding rejecter is now the frequency CAP, not confluence; WRAP slipped the dilution gate and is our only organic trade (-4%). Short-interest backlog item: setup-screen YES, breakout-chase NO.

**What the data says:**
- **Decision log — who's rejecting:** of 270 `skip_buy` rows the top reasons are frequency caps ("daily cap 4/3 reached" 138) and WRAP-specific bounces ("on avoid/intel list" 87, "max positions in Technology" 22, "failed confluence" 16). So confluence is NOT the throttle right now — the daily/weekly cap is, and the scorer keeps re-surfacing WRAP every cycle. We are capping out (demo posture 3/day-6/wk, min_score 5.25), i.e. the scorer produces more qualifying names than we can take.
- **WRAP leaked the dilution gate:** WRAP was bounced ~16× on `clean_dilution_history`/`momentum` confluence fails, then on 7/14 got IN at score 10.73 with 7/10 confluence — `clean_dilution_history` STILL failing — because `min_checks_passed:4` lets a 7/10 through and the fail-open critic approved it. WRAP is the exact dilution/promotional name the desk's own avoid list warns about, and it is now our **only organic trade: -4.04%** (`signal_rewards`, 10 rows/1 trade). `catalyst_rewards` still 0 rows → catalyst quality still unanswerable; don't invent it.
- **Every entry is an insider bet with news=0:** all 5 live buys (NTSK/UUUU/CAI/WRAP/YEXT) are insider-dominated (insider part 5–6 of a 7–11 score) with `news` part = **0.0** despite `news_weight:1.0`. The composite is effectively an insider engine; the news scorer contributes nothing at entry — worth a human check on whether news wiring is dead.
- **Regime/exits (unchanged verdict, still pending):** `quant_regime` = calm_drift (6 straight days, vol20 14.1% ann.), kmeans vs HMM disagree (HMM trending, persistence .954); CUSUM break 6/10, `tune_window_clean:false`. Walkforward PASSES (test +7.08% vs SPY +2.03%) but MC **p5 -8.9%** (fragile). Last night's TP 25→18 cut still stands and config is still 12/25 — not re-litigating, flagging it's unapplied.
- **Data trust:** 3 held broker-disagrees (WRAP +38.9%, NTSK +14.2%, YEXT +3.9%) all from a cache stale to 7/8 — broker is account of record, **cache stale not corrupt, trust broker**; WRAP's 38.9% gap means distrust any WRAP backtest rows. JEM $0.386 vs $6.04 = bad ticker-map, non-held, ignore. Survivorship 7.0% → backtest +40.3% is an upper bound.
- **Backlog rotation — short interest / float squeeze (verdict):** forum research (TradingSim, LuxAlgo days-to-cover, StockstoTrade, DayTradingToolkit) says the tradable squeeze needs low float + SI >15-20% + a catalyst, and the A+ *entry* is a breakout on **massive RVOL**. Our DNA is the opposite — all 6 buys sit at RVOL **0.24-0.83** (quiet accumulation). **Verdict: adopt the SETUP screen (yfinance `shortPercentOfFloat`/`floatShares`, free), reject the breakout CHASE.** Score the coiled spring while still quiet, never chase the pop.

**Proposed change (one, bounded):** Promote `clean_dilution_history` from a *failable* confluence check to a **hard confluence requirement** (a buy with it failing cannot pass, independent of `min_checks_passed`). Thesis: it's the one check that separates our clean insider names from the pump/dilution names the desk's own avoid list flags; WRAP proves the current 4-of-N gate + fail-open critic lets a known diluter through, and WRAP is our only realized trade and it lost. Bounded (touches no weights, no other veto), directly closes an observed leak. Validate by replaying the 98-trade sim with the hard gate on and confirming it doesn't cut the insider-into-weakness winners.
